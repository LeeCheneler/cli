{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": ["packages/*"]
  },
  "scripts": {
    "build": "lerna run build",
    "lerna:publish": "lerna publish from-package",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "typecheck": "lerna run typecheck"
  },
  "devDependencies": {
    "lerna": "^3.15.0"
  }
}
